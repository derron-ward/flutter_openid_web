name: flutter_web_auth
description: This package handles OpenID Connect authentication handling on Web
version: 1.0.0
homepage: https://github.com/derron-ward/flutter_web_auth

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  crypto: ^3.0.7
  dio: ^5.9.2
  flutter:
    sdk: flutter
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true